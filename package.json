{
	"name": "packwiz-website",
	"version": "1.0.0",
	"description": "Website for packwiz",
	"directories": {
		"doc": "docs"
	},
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "node scripts/build.js",
		"start": "npx nodemon -e yml,md,json,js -w scripts/build.js,mkdocs.yml,docs,packwiz-spec -x 'npm run build && serve site'",
		"format": "prettier -w ."
	},
	"author": "comp500",
	"license": "ISC",
	"dependencies": {
		"@material-design-icons/svg": "^0.14.2",
		"js-yaml": "^4.1.0",
		"kleur": "^4.1.4",
		"lightningcss": "^1.18.0"
	},
	"devDependencies": {
		"prettier": "^2.4.1"
	},
	"prettier": {
		"tabWidth": 4,
		"useTabs": true,
		"arrowParens": "avoid",
		"printWidth": 120,
		"overrides": [
			{
				"files": "*.yml",
				"options": {
					"tabWidth": 2,
					"useTabs": false
				}
			},
			{
				"files": "*.md",
				"options": {
					"tabWidth": 2
				}
			}
		]
	}
}
